## API SETUP PROCESS

### install xampp server for local testing after install start php and mysql 
### create a folder (LMS) into htdocs --> this is very important, into LMS folder have two folder one is API and another Frontend code folder (which have in repo)
